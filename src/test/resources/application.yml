spring:
  redis:
    host: localhost
    port: 6379

  jpa:
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: TRACE

server:
  tomcat:
    threads:
      max: 1000
      min-spare: 100